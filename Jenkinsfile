pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Use the 'git' step to clone the repository
                git 'https://github.com/Hemanthvk18/PetStoreRestAssured.git'
            }
        }
    }
}
